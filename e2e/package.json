{
  "name": "nagarikai-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end Playwright tests for the live nagarikai.xyz deployment.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
